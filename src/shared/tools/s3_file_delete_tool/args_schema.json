{
  "title": "S3FileDeleteArgs",
  "type": "object",
  "properties": {
    "file_path": {
      "type": "string",
      "description": "S3 file path to delete. Must point at a file, not a folder."
    }
  },
  "required": ["file_path"]
}
