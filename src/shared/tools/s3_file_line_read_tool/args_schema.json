{
  "title": "S3FileLineReadArgs",
  "type": "object",
  "properties": {
    "file_path": {
      "type": "string",
      "description": "S3 file path to read."
    },
    "offset": {
      "type": "integer",
      "description": "1-based line number to start reading from. Defaults to 1 (start of file)."
    },
    "limit": {
      "type": "integer",
      "description": "Maximum number of lines to return. Defaults to 2000. Use with offset to page through large files."
    }
  },
  "required": ["file_path"]
}
