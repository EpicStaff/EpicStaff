tool-data:
  name: "Notebook Edit Tool"
  description: "Replaces, inserts, or deletes a single cell in a Jupyter (.ipynb) notebook. notebook_path is relative to the sandbox root."
  args-schema: "args_schema.json"
  code-file: "main.py"
  entrypoint: "main"
  requirements: "requirements.txt"
