{
  "title": "S3FileEditArgs",
  "type": "object",
  "properties": {
    "file_path": {
      "type": "string",
      "description": "S3 file path to edit."
    },
    "old_string": {
      "type": "string",
      "description": "Exact text to replace. Must match the file content exactly, including whitespace. Must occur exactly once in the file unless replace_all is true."
    },
    "new_string": {
      "type": "string",
      "description": "Replacement text. Must differ from old_string."
    },
    "replace_all": {
      "type": "boolean",
      "description": "Replace every occurrence of old_string instead of requiring a single unique match. Defaults to false."
    }
  },
  "required": ["file_path", "old_string", "new_string"]
}
